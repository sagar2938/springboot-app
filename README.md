This project inclues apis to create task,get the status of the task and task details

Steps to run

./gradlew clean build
./gradlew bootrun

below url to access the swagger url of the apis
http://localhost:1234/swagger-ui.html
